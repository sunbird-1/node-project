pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                sh 'cd /var/lib/jenkins/workspace/build'
                sh 'sudo docker push rituparna1997/myimg:latest'
                
            }
        }
    }
}
